<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/thingColor"

    android:stretchColumns="1">

    <TableRow
        android:layout_marginBottom="@dimen/space_between_thing"
        android:layout_marginTop="@dimen/space_between_thing">
        <CheckBox
            android:id="@+id/done_checkbox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:focusable="false"
            android:layout_gravity="center"
            android:focusableInTouchMode="false" />

        <TextView
            android:id="@+id/name_tv"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:gravity="center"
            android:maxLines="2"
            android:textSize="@dimen/thing_txt_size"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/alarm_image"
            android:layout_width="@dimen/alarm_icon_size"
            android:layout_height="@dimen/alarm_icon_size"
            android:layout_gravity="center"
            android:src="@mipmap/alarm_icon"></ImageView>

        <TextView
            android:id="@+id/date_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginRight="2dp"
            android:textColor="@color/black"
            android:shadowRadius="30"
            android:textSize="@dimen/thing_txt_size"
            android:textStyle="bold" />
    </TableRow>
</TableLayout>
